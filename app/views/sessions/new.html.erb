<h1>Login</h1>

<%= form_tag sessions_path do |form| %>
  <div class="field">
    <%= label_tag :name %>
    <%= collection_select(:user, :id, User.all, :id, :name)%>
  </div>
  <br>
  <div class="actions">
    <%= submit_tag "Login" %>
  </div>
<% end %>